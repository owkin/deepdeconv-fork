## A test config to run the benchmark

deconv_methods: ["NNLS", "scVI"] # "TAPE", "Scaden", "scVI", "DestVI"
evaluation_datasets: ["BULK_FACS"]
granularities: ["FACS_1st_level_granularity"]
evaluation_pseudobulk_samplings: ["DIRICHLET"] # "UNIFORM", "PURIFIED"
n_samples_evaluation_pseudobulk: 500
n_cells_per_evaluation_pseudobulk: [256]
signature_matrices: ["FACS_1st_level_granularity"]
train_dataset: "CTI"
n_variable_genes: 2000
experiment_name: "scvi_highest_r2_genes_FACS_1st_gran" # if None, a random name will be chosen
save: True # if False, only the plots will be saved
